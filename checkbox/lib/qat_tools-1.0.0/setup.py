from __future__ import annotations
from setuptools import setup

setup(
    name="qat_tools",
    url="https://github.com/canonical/qat-tools",
    python_requires=">3.7",
    setup_requires=[
      "setuptools >= 61.0.0",
      "wheel",
    ],
)
